# Exercício 4

Uma temperatura, armazenada em $t0, pode ficar em dois intervalos:

20 ≤ temp ≤ 40 e
60 ≤ temp ≤ 80.

Escreva um programa que coloque uma flag (registrador $t1) para 1 se a
temperatura está entre os valores permitidos e para 0 caso contrário. Inicie o
código com a instrução: ori $t0, $zero, temperatura, substituindo temperatura
por um valor qualquer.
